export interface CategoryComparison {
  category: string;
  icon?: string;
  color?: string;
  totalAmount: number;
}

export interface YearlyTrend {
  year: number;
  income: number;
  expense: number;
}
